{% set name = "coiled" %}
{% set version = "0.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 329b88624af5db5cf8b3f865476dc6e5b9cb9e46369db21ca7ede59360d76b49

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - coiled = coiled.cli.core:cli

requirements:
  host:
    - python >=3.8
    - pip
    - wheel
    - hatchling
    - hatch-vcs
  run:
    - python >=3.8
    - aiohttp
    - backoff
    - boto3
    - click >=7.1
    - dask >=2.23.0
    - distributed >=2.23.0
    - filelock
    - importlib-metadata
    - ipywidgets
    - jmespath
    - jsondiff
    - pip >=19.3
    - prometheus_client
    - rich >=11.2.0
    - setuptools >=49.3.0
    - typing-extensions
    - wheel

test:
  requires:
    - pip
  commands:
    - pip check
    - coiled login --help
    - coiled env --help
    - coiled --version
  imports:
    - coiled

about:
  home: https://coiled.io
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Deploy Dask cluster using Coiled Cloud

  description: |
    A Python package to easily create, manage, and connect to Dask deployments
    using Coiled Cloud.
  doc_url: https://coiled.io/cloud

extra:
  recipe-maintainers:
    - dan-blanchard
    - ntabris
    - shughes-uk
    - dchudz
    - jrbourbeau
    - mrocklin
