{% set name = "coiled" %}
{% set version = "0.5.15" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 93c589e4b0e560bed68e403c54667aa1126e8b42b00f84043acd0d9db5b1ffe6

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - coiled = coiled.cli.core:cli

requirements:
  host:
    - python >=3.7
    - pip
    - wheel
    - hatchling
    - hatch-vcs
  run:
    - python >=3.7
    - aiohttp
    - boto3
    - click >=7.1
    - dask >=2.23.0
    - rich >=11.2.0
    - typing-extensions
    - backoff
    - jmespath
    - jsondiff
    - importlib-metadata
    - pip >=19.3
    - setuptools
    - wheel
    - ipywidgets
    - prometheus_client
    - filelock >=3.0
test:
  requires:
    - pip
  commands:
    - pip check
    - coiled login --help
    - coiled env --help
    - coiled --version
  imports:
    - coiled

about:
  home: https://coiled.io
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Deploy Dask cluster using Coiled Cloud

  description: |
    A Python package to easily create, manage, and connect to Dask deployments
    using Coiled Cloud.
  doc_url: https://coiled.io/cloud

extra:
  recipe-maintainers:
    - dan-blanchard
    - ntabris
    - shughes-uk
    - dchudz
    - jrbourbeau
    - mrocklin
